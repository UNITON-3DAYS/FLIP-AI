# 기본 (selftest와 채점 로직에 필수)
numpy
opencv-python-headless
pillow
sympy
requests

# OCR·PDF 준비용(paddle 등)은 requirements-ocr.txt로 분리했다 — 무겁고,
# 서버는 fullpage 그레이더라 OCR을 안 쓴다. 로컬 blocks 채점 시에만 깐다.

# 채점 서버 (api/). Spring이 base64 사진을 POST /grade로 보내 동기 채점.
fastapi
uvicorn[standard]

# 정답 소스 MySQL 백엔드 (flip/answers.py). Backend와 같은 GCP MySQL을 읽는다.
# 순수 파이썬이라 시스템 빌드 의존이 없다(mysqlclient 대비 슬림).
PyMySQL
